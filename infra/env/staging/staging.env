# Non-secret configuration — single source of truth for BOTH backend and frontend (env: staging).
#
# Backend: Terraform sets these as Lambda env vars from this file. Frontend: baked via `vite --mode staging`.
# Secrets are NEVER stored here (Secrets Manager). This file is committed.
ENV=staging
API_BASE_URL=/api
MAGIC_LINK_URL_PARAM=ml
