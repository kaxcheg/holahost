# Non-secret configuration — single source of truth for BOTH backend and frontend (env: prod).
#
# Backend: Terraform sets these as Lambda env vars from this file. Frontend: baked via `vite --mode prod`.
# Secrets are NEVER stored here (Secrets Manager). This file is committed.
ENV=prod
API_BASE_URL=/api
MAGIC_LINK_URL_PARAM=ml
