# Frontend-owned magic-link URL contract (source of truth) — invariant constants, NOT per-env.
# COMMITTED, non-secret. Read by vite (baked into the bundle) and by Terraform (relayed to the backend
# Lambda, I-12). See the spec §10.9 (Settings inventory).
MAGIC_LINK_PATH=/claim
MAGIC_LINK_URL_PARAM=ml
