# NOTE: the frontend does NOT read config from a local .env.
# The single source of non-secret config is infra/env/<env>/<env>.env (shared with the backend),
# baked into the bundle at build time via `vite --mode <env>`.
# See frontend/vite.config.ts (loadDeployEnv) and frontend/src/config.ts.
